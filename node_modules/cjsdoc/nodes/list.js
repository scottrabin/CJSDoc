exports.NODE_TYPE = "LIST";

exports.initialize = function( leaf, node, root ){
    leaf.children = [];
};

exports.handle = function( leaf, node, root ){
    // this is a list type element; it keeps children
    leaf.name = "blank";
};

exports.attachChild = function( parent, child ){
    parent.children.push( child );
};