exports.NODE_TYPE = 'FUNCTION';

exports.handle = function( leaf, node, root ){
    leaf.name = node.name;
    // loop through the params to make them objects
    leaf.params = node.params.slice();
};